# Variables para el build de produccion (Cloudflare Pages).
# Copiar a .env.production (o configurarlas como "Environment variables"
# del proyecto en Cloudflare Pages) antes de correr `npm run build`.

# URL publica del backend, con el prefijo /api
VITE_API_BASE_URL=https://api.automatedsolutionsgroup.org/api
